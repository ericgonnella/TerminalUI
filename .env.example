# Credentials for scripts/seed-external.js.
# Copy this file to .env (which is gitignored) or export the vars in your shell
# before running the seed script. The script will refuse to run without
# PG_PASSWORD set.
PG_HOST=127.0.0.1
PG_PORT=5433
PG_USER=postgres
PG_PASSWORD=
PG_DATABASE=postgres
